[package]
name = "astrid-capsule-router"
version = "0.1.0"
description = "Tool execution routing middleware for Astrid OS"
authors = ["Astrid Core Team"]
astrid-version = ">=0.1.0"

[[component]]
id = "router"
file = "astrid_capsule_router.wasm"
type = "executable"

[capabilities]
ipc_publish = ["tool.v1.execute.*", "tool.v1.execute.result"]

# Tool execution requests from the react loop
[[interceptor]]
event = "tool.v1.request.execute"
action = "handle_execute_request"

# Tool results from tool capsules (wildcard match on tool.v1.execute.*.result)
[[interceptor]]
event = "tool.v1.execute.*.result"
action = "handle_execute_result"
